interface:
  display_name: "ChatGPT Desktop Bridge"
  short_description: "Check or use supported desktop ChatGPT route"
  default_prompt: "Use $chatgpt-desktop-bridge to check whether my registered ChatGPT desktop endpoint is supported and route this task only if it is ready."
policy:
  allow_implicit_invocation: true
